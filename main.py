# -*- coding: utf-8 -*-
"""main.py: 统一运行总入口"""

import sys
from downloader import BBCResourceDownloader


def main():
    # 初始化模块化下载器引擎
    engine = BBCResourceDownloader()

    print("==================================================")
    print("      🚀 中国学生外刊精读资源库分发底座 🚀         ")
    print("==================================================")

    # ----------------------------------------------------------------
    # 【模式选择一：历史全量收割模式】
    #  功能：全自动捞取自 2020 年起到 2026 年的所有历史文章。
    #  红利：自带科学断点续爬，本地有了会自动秒闪过，绝不重复下载。
    # ----------------------------------------------------------------
    engine.download_history(start_year=2020, end_year=2026)

    # ----------------------------------------------------------------
    # 【模式选择二：每日最新增量同步模式】
    #  功能：网站上线后的日常运维。每天早上跑一次，只抓最近 1~2 天官方更新、
    #        且本地已下载资产中没有出现的全新文章，保护网站良性更新。
    # ----------------------------------------------------------------
    # engine.sync_latest()


if __name__ == "__main__":
    try:
        main()
    except KeyboardInterrupt:
        print("\n🛑 用户手动终止了抓取管道，安全退出。")
        sys.exit(0)